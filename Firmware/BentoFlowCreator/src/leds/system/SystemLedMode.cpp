
#include "SystemLedMode.h"

SystemLedMode::SystemLedMode() :
   Component("systemLedMode")
{
    
}

bool SystemLedMode::handleCommand(String command, var * data, int numData) 
{
   return false; 
}
